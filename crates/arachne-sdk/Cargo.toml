[package]
name = "arachne-sdk"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Typed Rust client for Arachne's secure peer-to-peer workspaces"
repository = "https://github.com/arachne-systems/arachne-sdk"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
arachne-runtime = { path = "../../core/crates/arachne-runtime" }
